# Stripe (add when payments are wired)
STRIPE_PUBLIC_KEY=pk_live_REPLACE_ME
STRIPE_SECRET_KEY=sk_live_REPLACE_ME_NEVER_EXPOSE_THIS

# Email provider (add when forms are wired)
SENDGRID_API_KEY=SG.REPLACE_ME

# Google Search Console
GOOGLE_SITE_VERIFICATION=REPLACE_WITH_YOUR_CODE
