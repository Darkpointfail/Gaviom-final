{
  "name": "gaviom-v2",
  "private": true,
  "scripts": {
    "images": "node scripts/optimize-images.mjs --source",
    "build": "node scripts/build-blog.mjs && node scripts/build-static.mjs && node scripts/optimize-images.mjs",
    "dev": "npx --yes serve -l 3001 .",
    "preview": "npm run build && npx --yes serve -l 3001 dist",
    "start": "npm run dev"
  },
  "devDependencies": {
    "sharp": "^0.34.2"
  }
}
