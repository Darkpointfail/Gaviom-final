/** Supabase public keys — safe in the browser. Replace placeholders before going live. */
window.GAVIOM_AUTH_CONFIG = {
  supabaseUrl: 'REPLACE_WITH_SUPABASE_URL',
  supabaseAnonKey: 'REPLACE_WITH_SUPABASE_ANON_KEY',
};
